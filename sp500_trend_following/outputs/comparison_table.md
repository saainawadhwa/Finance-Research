# Trend Following vs Buy and Hold Comparison Table

The live table could not be populated in the original sandbox because public market-data hosts could not be resolved. After running the backtest with online data or a local SPY CSV, `outputs/report.md` and `outputs/summary.csv` will contain the populated values for this table.

| Strategy | Final after-tax value | Total return | CAGR | Max drawdown | Trades | Fees | Taxes |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| trend following | data needed | data needed | data needed | data needed | data needed | data needed | data needed |
| buy and hold | data needed | data needed | data needed | data needed | data needed | data needed | data needed |

Run from the `sp500_trend_following/` folder:

```bash
python3 backtest_sp500_trend.py --start 2021-07-10 --end 2026-07-10 --output-dir outputs
```
